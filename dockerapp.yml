server:
  port: 8080

eureka:
  instance :
    prefer-ip-address: true
  client:
    enabled : true
    register-with-eureka : true
    fetch-registry : true
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/
sample :
    message : Message From Config Server
