server:
  port: 8086

eureka:
  instance :
    prefer-ip-address: true
  client:
    enabled : true
    register-with-eureka : true
    #fetch-registry : true
    serviceUrl:
      defaultZone: http://uopenserviceregistry:8761/eureka
sample :
    message : Message From Config Serversss